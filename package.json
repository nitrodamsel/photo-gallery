{
  "name": "image-gallery",
  "version": "1.0.0",
  "description": "Image gallery application with tagging and thumbnail support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node scripts/migrate.js",
    "migrate:undo": "node scripts/migrate.js --undo",
    "seed": "node scripts/seed.js",
    "seed:undo": "node scripts/seed.js --undo"
  },
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "sequelize": "^6.35.2",
    "sqlite3": "^5.1.6",
    "umzug": "^3.4.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}